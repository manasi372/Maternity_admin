<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog Slider</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        /* ===== EDIT BLOG SLIDER STYLES ===== */

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }

        .edit-container {
            max-width: 600px;
            margin: auto;
            padding: 20px;
            background: white;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            font-size: 26px;
            font-weight: bold;
            color: #007bff;
            text-align: center;
            margin-bottom: 20px;
        }

        .edit-form .form-label {
            font-weight: bold;
        }

        .edit-form .form-control {
            border-radius: 5px;
        }

        .edit-form .btn-primary {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
        }

        .current-img {
            display: block;
            width: 100%;
            max-width: 250px;
            margin: 10px auto;
            border-radius: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        }

        .btn-secondary {
            display: block;
            width: 100%;
            margin-top: 10px;
        }

    </style>
</head>
<body>

    <div class="container mt-4">
        <div class="edit-container">
            <h2>Edit Blog Slider</h2>

            <!-- Blog Edit Form -->
            <form action="/admin/update_blogslider/<%= blogslider.blogslider_id %>" method="POST" enctype="multipart/form-data" class="edit-form">
                
                <div class="mb-3 text-center">
                    <label class="form-label">Current Image</label><br>
                    <img src="/admin_assets/<%= blogslider.image %>" alt="Current Blog Image" class="current-img">
                </div>

                <div class="mb-3">
                    <label class="form-label">Upload New Image</label>
                    <input type="file" name="image" class="form-control">
                </div>

                <button type="submit" class="btn btn-primary">Update</button>
                <a href="/admin/blogslider" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>

</body>
</html>

<%- include("footer.ejs") %>
