<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Contact Source</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .form-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
        .img-preview {
            width: 100px;
            height: 100px;
            object-fit: cover;
            display: block;
            margin: 10px auto;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="form-container">
        <h2 class="text-center mb-4 text-primary">Edit Contact Source</h2>
        <form action="/admin/update_contactcards/<%= contactsource.contactsource_id %>" method="POST" enctype="multipart/form-data">
            
            <div class="mb-3">
                <label class="form-label">Heading</label>
                <input type="text" name="heading" class="form-control" value="<%= contactsource.heading %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Info 1</label>
                <input type="text" name="info1" class="form-control" value="<%= contactsource.info1 %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Info 2</label>
                <input type="text" name="info2" class="form-control" value="<%= contactsource.info2 %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Current Image</label>
                <img src="/admin_assets/<%= contactsource.image %>" class="img-preview" alt="Current Image">
            </div>

            <div class="mb-3">
                <label class="form-label">Upload New Image</label>
                <input type="file" name="image" class="form-control">
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-success">Update</button>
                <a href="/admin/contactcards" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>

<%- include("footer.ejs") %>
