<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .form-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
        .img-thumbnail { width: 120px; height: 120px; border-radius: 5px; object-fit: cover; }
    </style>
</head>
<body>

<div class="container">
    <div class="form-container">
        <h2 class="text-center mb-4 text-primary">Edit Gallery Item</h2>
        <form action="/admin/update_gallery/<%= gallery.gallery_id %>" method="POST" enctype="multipart/form-data">
            
            <!-- Existing Image Preview -->
            <div class="text-center mb-3">
                <% if (gallery.image) { %>
                    <img src="/admin_assets/<%= gallery.image %>" class="img-thumbnail">
                <% } else { %>
                    <p>No Image</p>
                <% } %>
            </div>

            <!-- Hidden input to store old image -->
            <input type="hidden" name="old_image" value="<%= gallery.image %>">

            <!-- Image Upload -->
            <div class="mb-3">
                <label class="form-label">Upload New Image</label>
                <input type="file" name="image" class="form-control">
            </div>

            <!-- Gallery Heading -->
            <div class="mb-3">
                <label class="form-label">Gallery Heading</label>
                <input type="text" name="gallery_heading" class="form-control" value="<%= gallery.gallery_heading %>">
            </div>

            <!-- Gallery Information -->
            <div class="mb-3">
                <label class="form-label">Gallery Information</label>
                <textarea name="gallery_info" class="form-control" rows="3"><%= gallery.gallery_info %></textarea>
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-success">Update</button>
                <a href="/admin/galleryslider" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>

<%- include("footer.ejs") %>
