<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Countdown Event</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f4f4f4; }
        .container { max-width: 85%; }
        .form-box {
            background: white; padding: 20px; border-radius: 8px;
            box-shadow: 2px 4px 15px rgba(0, 0, 0, 0.15);
        }
        .img-thumbnail { width: 80px; height: 80px; border-radius: 8px; object-fit: cover; }
    </style>
</head>
<body>

<div class="container mt-4">
    <!-- Edit Countdown Form -->
    <div class="form-box">
        <h5 class="text-primary text-center">Edit Countdown Event</h5>
        <form action="/admin/update_countdown/<%= countdown.countdown_id %>" method="post" enctype="multipart/form-data">
            <input type="hidden" name="old_image" value="<%= countdown.image %>">
            
            <div class="mb-3">
                <label class="form-label fw-bold">Event Name</label>
                <input type="text" name="name" class="form-control" value="<%= countdown.name %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Event Info</label>
                <input type="text" name="info" class="form-control" value="<%= countdown.info %>">
            </div>

            <div class="mb-3">
                <label class="form-label">Event Image</label>
                <input type="file" name="image" class="form-control">
                <div class="mt-2">
                    <img src="/admin_assets/<%= countdown.image %>" class="img-thumbnail">
                </div>
            </div>

            <button type="submit" class="btn btn-primary w-100">Update Event</button>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<%- include("footer.ejs") %>
